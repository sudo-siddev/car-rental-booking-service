# Backend Environment Variables
# These are optional - the application has sensible defaults for local development

# Frontend Origin for CORS (optional)
# Default: http://localhost:5173 (used if not set)
# FRONTEND_ORIGIN=http://localhost:5173

# For production deployment, set to your frontend URL:
# FRONTEND_ORIGIN=https://your-frontend-domain.com

# Spring Profile (optional)
# Default: dev (configured in application.properties)
# SPRING_PROFILES_ACTIVE=prod
